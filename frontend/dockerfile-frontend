# Dockerfile.frontend (coloque na raiz do projeto)

# Estágio 1: Build da Aplicação Angular
FROM node:18-alpine AS build

WORKDIR /app

# --- ALTERAÇÃO AQUI ---
# Copia os arquivos de dependência que estão DENTRO da pasta /frontend
COPY package*.json ./

RUN npm install

# --- ALTERAÇÃO AQUI ---
# Copia todo o código-fonte que está DENTRO da pasta /frontend
COPY . .

RUN npm run build -- --configuration production

# Estágio 2: Servir a Aplicação com Nginx
FROM nginx:1.25-alpine

# Copia seu arquivo de configuração Nginx (que também deve estar na raiz do projeto)
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Copia os arquivos compilados do estágio de build.
# O nome 'cliente-app' deve estar correto conforme seu projeto.
# Esta linha NÃO MUDA, pois o caminho é DENTRO do container do estágio anterior.
COPY --from=build /app/dist/crud-angular/ /usr/share/nginx/html

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]